---
layout: default
title:  Home
---

.loop-wrap
  .masonry

    {% for post in site.posts %}

    a(class="masonry__brick {{ post.category }}" href="{{ post.url }}" style="height: {{ post.height }};")

      p.title {{ post.title }}

    {% endfor %}